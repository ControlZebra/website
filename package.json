{
  "name": "controlzebra-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ControlZebra marketing website built with Astro",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "pages:dev": "npm run build && wrangler pages dev dist",
    "pages:deploy": "npm run build && wrangler pages deploy"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.37.0",
    "astro": "^5.4.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "typescript": "^5.9.3",
    "wrangler": "^4.10.0"
  }
}
